<%= debug params %>

<div class="container mt-2">
  <div class="row">
    <div class="col-md-6 col-md-offset-3">
      <article class="panel panel-default">
        <header class="panel-heading clearfix">
          <h4 class="pull-left">
            <%= params[:vendor].parameterize %>_<%= Time.now.strftime("%m-%d-%Y") %>
            <span class="badge">Draft</span>
          </h4>
          <%= link_to "« Return to Index", "/proto/index", class:"btn btn-link pull-right" %>
        </header>
        <section class="panel-body">
          <div class="panel panel-info">
            <div class="panel-heading">
              <%= params[:vendor].parameterize %>_<%= Time.now.strftime("%m-%d-%Y") %>
            </div>
            <div class="panel-body row">
              <div class="col-md-6 mb-1">
                <%= link_to "Create new style in draft", "/proto/new_style", class:"btn btn-primary" %>
              </div>
              <div class="col-md-6 mb-1">
                <%= link_to "Add existing style to draft", "/proto/existing_style", class:"btn btn-primary" %>
              </div>
            </div>
          </div>

          <div class="row">
            <div class="col-md-6">
              <%= params[:vendor] %>
            </div>
            <div class="col-md-6">
              <%= params[:start_ship] %> - <%= params[:end_ship] %>
            </div>
          </div>
          <hr>
          <div class="row">
            <div class="col-md-6 mb-1">
              <%= link_to "Create new style in draft", "/proto/new_style", class:"btn btn-primary" %>
            </div>
            <div class="col-md-6 mb-1">
              <%= link_to "Add existing style to draft", "/proto/existing_style", class:"btn btn-primary" %>
            </div>
          </div>
        </section>
      </article>
    </div>
  </div>
</div>
